#include<stdio.h>
int main(){
        int a, sum=0;
        for(int i=0; i<7; i++){
                scanf("%d",&a);
                sum+=a;
        }
        printf("%d %d %d",sum/100,sum/10-sum%10,sum-sum%100);
        return 0;
}
